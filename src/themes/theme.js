import { createTheme } from "@mui/material/styles";

export const appTheme = createTheme({
    palette: {
        primary: {
            main: '#005DBD'
        },
        secondary: {
            main: '#F58D12'
        },
        default: {
            main: '#000000'
        },
        warning: {
            main: '#FAC935'
        }
    },
});